# autotool-plugin-recursive-2

This plugin is added by `autotool-plugin-recursive-1` after a recursion. This
plugin also removes itself and `autotool-plugin-recursive-1`, cancelling both of
their effects out.
